# .env.dev

# 后端端口号
VITE_BACKEND_PORT=3001

# 前端端口号
VITE_FRONTEND_PORT=3000

# 项目前缀
APP_KEY_PREFIX=dev_

# --- 数据库配置 (核心修正) ---
# 变量名已更新，以匹配 PostgreSQL 官方 Docker 镜像的要求。
# 这使得我们可以直接通过 `env_file` 指令加载，无需额外映射。

# 数据库主机 (后端服务连接时使用)
DB_HOST=localhost
# 如果在docker-compose.dev.yml中运行后端，请将DB_HOST更改为'postgres'

# 数据库端口 (后端服务连接时使用)
DB_PORT=5432

# PostgreSQL 容器初始化时使用的变量
POSTGRES_PORT=5432
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=message_center_dev
