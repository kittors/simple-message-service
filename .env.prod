# .env.prod

# --- 应用配置 ---
NODE_ENV=production
# 后端服务端口
VITE_BACKEND_PORT=8081
# 项目前缀
APP_KEY_PREFIX=prod_

# --- 数据库连接配置 (供后端应用使用) ---
# 核心修正：DB_HOST 必须是 docker-compose.yml 中定义的数据库服务名。
DB_HOST=postgres
DB_PORT=5432
DB_USER=produser
DB_PASSWORD=prodpassword
DB_NAME=message_center_prod

# --- 数据库初始化配置 (供 PostgreSQL 容器自身使用) ---
# 这些变量被 PostgreSQL 镜像用来在首次启动时创建数据库和用户。
# 为了保持一致性，我们让它们的值与上面的 DB_* 变量相同。
POSTGRES_USER=produser
POSTGRES_PASSWORD=prodpassword
POSTGRES_DB=message_center_prod
