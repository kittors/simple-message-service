# .env.example

# --- 应用配置 ---
NODE_ENV=production
VITE_BACKEND_PORT=8081
APP_KEY_PREFIX=prod_

# --- 数据库连接配置 (供后端应用使用) ---
# 在生产 Docker 环境中，这里应该填写数据库服务的名称，例如 'postgres'。
DB_HOST=postgres
DB_PORT=5432
DB_USER=produser
DB_PASSWORD=prodpassword
DB_NAME=message_center_prod

# --- 数据库初始化配置 (供 PostgreSQL 容器自身使用) ---
POSTGRES_USER=produser
POSTGRES_PASSWORD=prodpassword
POSTGRES_DB=message_center_prod
